# @dpd/shared

Shared TypeScript contracts between `@dpd/api` and `@dpd/web`.

## Exports

- Deployment and pod summary types
- Namespace types
- `ApiErrorCode` enum for client-side error translation

## Build

```bash
pnpm build
```

Consumed as `workspace:*` dependency — no publishing required.
