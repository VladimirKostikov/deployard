# Weather Station

API and static web UI.

| Service | Role |
|---|---|
| `weather-api` | Weather JSON API |
| `weather-web` | Browser UI |

## Local Docker

```bash
cd demo/weather-station
docker compose up --build -d
```

| URL | Description |
|---|---|
| http://localhost:3301 | Web UI |
